# MCP-Local 核心讀取效能優化驗證報告

日期：2026-09-13。範圍：規格第 22 節的一輪核心修改（OPT-03、04、07，OPT-02 本機契約驗證）。
未修改正式 Root／default_root、工具 schema、契約版本或服務版本標記。OPT-01、05、06、08～11 留待後續批次；本報告不代表整份規格全部完成。

## 修改與安全

- walk 使用已驗證父目錄下的 DirEntry 名稱與 metadata 分類；子目錄進入前及 scandir 批次後保留父路徑 checked。
- Windows DirEntry 的 st_nlink 可能是 0；regular file 遇到此值必須再以 Path.lstat 取得硬連結資訊，拒絕非單一硬連結。這是安全所需的平台修正，不能直接採用未補查的快取資料。
- 列表透過私有 _with_info 使用本輪 metadata，免除逐筆 checked 與 is_dir 查詢；預設 walk 仍回傳 Path。
- 搜尋使用同一 walk，移除開啟前重複 checked／stat；open_checked 仍驗證大小、身分與安全屬性。
- AST 比對確認 checked、open_checked、read_file 三個方法與備份完全一致。Snapshot 仍只是名稱快照。
- 新增 7 項回歸，包含名稱、Hidden/reparse/symlink/hardlink metadata、零連結數補查、pending 子目錄重驗、Snapshot 硬連結替換與搜尋前刪除。既有消失檔案測試改在新的 entry 驗證邊界注入，避免依賴被移除的第二次 checked 次數。

## 合成基準（三次中位數）

各資料集另含 1 個 1,950,000 bytes 大檔，故實際列舉為 1,001／10,001 檔。相同最終 benchmark_performance.py、Python 3.10.11、相同資料；未清除 OS cache，先舊版再新版。啟用 tracemalloc 及 Python 呼叫 hooks，時間不可直接等同比無 instrumentation 的實際 Root。

| 資料 | 首頁 ms 舊→新 | Path.stat 舊→新 | Path.lstat 舊→新 | DirEntry.stat 舊→新 | 第二頁 ms 舊→新 |
|---|---:|---:|---:|---:|---:|
| 1k | 1043.735 → 161.603 | 7012 → 1007 | 4007 → 1005 | 0 → 1001 | 0.903 → 0.895 |
| 10k | 10618.056 → 1656.796 | 70012 → 10007 | 40007 → 10005 | 0 → 10001 | 1.135 → 0.935 |

Path.lstat 內部也會呼叫 Path.stat，因此兩欄是 Python 方法計數，不能相加宣稱 OS 系統呼叫次數。DirEntry.stat 另列，避免將改走 DirEntry 誤報成零 metadata 查詢。
1k／10k 兩版首次列舉 read_bytes=0、skipped_entries=0，scanned_entries/sorted_entries 相同。完整 samples、各項 read_bytes、peak、搜尋 scanned_bytes 見 JSON。
搜尋 limit=1 只讀取 7 bytes；主要時間仍有目錄批次排序成本，此案例沒有宣稱搜尋大幅加速。

## 配置取捨

- 1k 首頁 Python peak：617,697 → 1,114,667 bytes；DirEntry metadata 快取提高配置。
- 10k 首頁 Python peak：6,078,816 → 11,073,597 bytes；DirEntry metadata 快取提高配置。
- 大檔 read_window 仍完整讀取 1,950,000 bytes，Python peak 約 219 KB；此次未實作小檔快速路徑，不能宣稱 read_file CPU 改善。

## 既有共享 Root（三次中位數）

不修改設定、不讀取內容，只列舉既有 default Root；不輸出根的絕對路徑或檔名清單。wall-clock only，未啟用 tracemalloc，未清除 OS cache。
- 首頁：17,467.505 → 6,618.482 ms，縮短 62.1%。
- 六輪均掃描 41,416 項、排序 9,533 檔、略過 22,711 項；scan_truncated=false。
- 第二頁新版中位數：0.275 ms。

## 回歸與契約

- python -m py_compile local_files_mcp.py：通過。
- python -B -m unittest discover -s tests -v：123 tests、0 failure、0 error、0 skip。
- python -B verify_isolated.py：123 tests、0 failure、0 error、0 skip；pip check=0。
- MCP 1.30.0；本機 STDIO 單 Root／多 Root 列工具均驗證完整 tool-contract.json 與 readOnlyHint，八工具通過。Snapshot 多頁 walk.call_count=1 回歸通過。
- 最終用戶端本次對話仍暴露舊版三工具，不能宣稱八工具遠端刷新完成；完整遠端契約驗收尚未完成。

## 連線

已沿用原設定重新啟動既有登入工作。排程停止未結束舊的脫離程序，核對 PID 與本專案 autostart.py 後結束該程序樹，再啟動原排程。新 tunnel-client PID 為 31996（驗證當時），不是正式遠端八工具驗收。

## 追溯與重跑

- 修改前備份：performance-backup-20260913-180807/。
- 合成：python -B benchmark_performance.py --source performance-backup-20260913-180807 --files 1000 --output performance-before-1k.json；新版省略 --source；10k 改 --files 10000。
- 實際 Root：python -B benchmark_actual_performance.py（讀取當時現有設定；後續設定改變會影響可比性）。
- 50k／100k 壓力驗證未執行；不宣稱敵對本機寫入者隔離。

## 產物 SHA-256

- local_files_mcp.py: 97bfddc0a7b19e72c3005255bfa7f4954f5afcf0fc84e4bdbb80196b62724db5
- tests/test_performance_safety.py: 09bf56277273cb25bbc7a1263f38bf07d2720ff33d691be597b7d9be93bebeaa
- tests/test_pagination.py: cb545be2b2e950c7babf4f78d17513d9804abfcbb466f62b4025816ed9baf940
- benchmark_performance.py: cf04b920627a3f28bd2a09e14d7b7865ed9046875a5b0e19364c17c59e3740ce
- benchmark_actual_performance.py: b3c71774c8370c15a77c2a9d9774a384197c90d9e2b974b0a3db9f794a3a8025
- performance-before-1k.json: 41fb0094552458bcf7e31ea9c3958d805cc4eb707cadc1fd5d7243af373b7e3b
- performance-before-10k.json: cc008dd06e7b7e7498d08151df03dfab4280ad2dd22098d422cc0db59a7b8be0
- performance-after-1k.json: 09fa4e3f42b0cb11f4572cc188aa2fe03bed55907220dee1c645bfc4d89fdf17
- performance-after-10k.json: 2a6be5f000bd34da88f05fb125419c39a852a7f299793a61965add164e7d77cd
- performance-actual.json: b0e279538a957b8d293a8a5a3317b0b0866675a1dbe74d56107d23061a95a641

## 重連後遠端煙霧測試

重連後第一次呼叫 HTTP 504；重試 list_files(directory=MCP-Local, limit=1) 成功：scanned_entries=124、sorted_entries=112、elapsed_ms=18.695。已確認遠端列舉恢復，但未宣稱八工具最終用戶端刷新完成。

