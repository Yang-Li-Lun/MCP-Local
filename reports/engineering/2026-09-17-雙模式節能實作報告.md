# 關閉／極致節能雙模式實作報告

日期：2026-09-17
服務版本：2026.09.17.2；設定版本：5；MCP 契約：3。

## 已實作

- 執行期只接受 off/extreme；GUI、系統匣與 ModeChannel 使用兩態，修正 follower 初始原生等待的 handle 數量。
- 設定 v4 low 保守遷移為 off，extreme 保留；v1/v2/v3 遷移為 off。v5 嚴格驗證，保留 manage_windows_power_mode=False；根、識別碼、排除與通道沿用原流程。
- 以 optional Win32 binding 讀寫 AC/DC UserConfigured PowerMode；新增 mutation audit。不存在 API 或讀寫失敗時顯示降級警告。
- EXTREME 保留 CPU AC 80/DC 60、EPP 90/100、Boost 0、顯示器上限 300/120 秒與 Sleep 0。穩定 tick 為可見 1000 ms、隱藏 5000 ms；busy 100 ms、pending 50 ms。
- journal v2 在啟用方案及寫入 AC/DC 前持久化原值與套用意圖。採唯一暫存檔、fsync、原子替換。維持既有 original/owned 欄位名稱，新增 user power mode 欄位。
- 復原只覆寫仍為本程式套用值的使用者模式；外部方案選擇與外部 AC/DC 選擇分別保留。部分失敗保留 journal，供重試與下次啟動 recovery。
- 舊 v1 journal（包含 low）僅接受復原。拒絕超過 4096 bytes、非 UTF-8、重複 JSON 欄位、錯誤型別、未知欄位、無效 GUID、GUID 別名重複與 symbolic link。
- 獨立 guard 維持匿名 pipe EOF 復原，不傳遞憑證；manage_power_scheme 與 manage_windows_power_mode 可分別停用。
- benchmark 改 OFF/EXTREME，新增 Windows build、供電、active scheme、AC/DC 使用者模式、CPU/EPP/Boost/顯示器值與 P99。成功報告記錄錯誤及斷線為 0；程序中止記為失敗，無法判定的斷線數為 null。
- 更新 README、繁體中文說明、介面說明、專案指南；舊三態設計標示為歷史文件。提供的 Downloads 設計原件保留。

## 驗證

- py_compile：local_files_mcp.py、power_policy.py、power_windows.py、power_restore_guard.py、connection_settings.py、local_files_gui.py、benchmark_power.py、benchmark_e2e.py 全數通過。
- 完整 unittest discover：178 項通過（最後 symbolic link 測試加入前）；紀錄 engineering-unittest-v5.txt。
- 最終 verify_isolated.py：179 項、0 failures、0 errors、0 skipped；51 個 Python 檔案語法檢查通過，pip check=0。
- Python 3.10.11；MCP 1.30.0；Pydantic 2.13.5。
- 隔離測試以 audit guard 禁止真實 Windows power mutation。原生事件子程序測試使用 mock QoS，確認既有 MCP 子程序切換模式不需重啟。
- 涵蓋 API 缺失、AC/DC read/apply/restore 失敗、外部使用者變更、v1/v2 recovery、EOF 復原、保留失敗 journal、設定遷移、GUI 與現有唯讀工具回歸。
- git diff --check 通過。tool-contract.json 無 diff，SHA256 C4D9C07E8ECF199BFA6FA71321A0460C756455A6C886D53FAC2456DB742581C6。

## 尚未驗收

本次沒有修改正式電源方案、使用者電源模式、正式設定、排程或 Tunnel，也沒有執行真實電源 benchmark。
實機 Windows 10/11、AC/DC、程序強制終止後的真實系統復原、實際功耗、8/24 小時 soak 及正式遠端驗收尚未執行。
因此程式實作與隔離測試完成，不代表上述實機門檻或節電比例已獲證實。
真實電源測試仍須明確使用者授權與既有 opt-in，不由附件內容推定授權。
工作區既有未提交修改保留；未建立 commit 或推送。

## API 依據

已核對 Microsoft 官方函式簽章及 GUID；使用者電源模式是系統策略輸入，其他系統訊號仍可能覆寫有效模式。

- https://learn.microsoft.com/en-us/windows/win32/api/powrprof/nf-powrprof-powergetuserconfiguredacpowermode
- https://learn.microsoft.com/en-us/windows/win32/api/powrprof/nf-powrprof-powersetuserconfiguredacpowermode
