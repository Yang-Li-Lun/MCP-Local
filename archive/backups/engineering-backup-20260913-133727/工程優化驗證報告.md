# 工程優化驗證報告

日期：2026-09-13。設計來源為使用者提供的 `MCP-Local_工程優化與開機自啟動設計.txt`，原文件保留。

## 已實作

Connection 核心抽離、錯誤分類與退避策略、設定 v3、登入背景程序與 Task Scheduler 管理、GUI 自啟動及背景控制、安全輪替日誌、Root 重疊拒絕、備份輪替去重、SHA-256 啟動驗證、記憶體快照分頁、區塊串流搜尋、HTTPS 遠端驗收工具與效能／週期負載工具。

八個唯讀 MCP 工具名稱與主要介面保留；未增加寫入、刪除或執行命令工具。未修改現有分享設定、DPAPI 金鑰或正式排程，也未停止目前連線。測試連線採獨立 Mutex，避免干擾使用中的正式 Mutex。

## 測試結果

- `python -B -m unittest discover -s tests -v`：62 項通過，29.702 秒。
- 後續加入遠端核准參數防護測試，`test_optimization.py` 共 14 項通過，2.192 秒；合計已通過 63 個不同測試案例。
- 所有專案與測試 Python AST／py_compile 語法檢查通過；`pip check` 通過。
- 原有 Windows Hidden、symlink、junction、hardlink、UTF-8、NUL、容量、游標、Root 隔離、DPAPI、Job、GUI／系統匣及八工具 STDIO 回歸通過。
- 新增快照掃描次數、過期、LRU／容量、設定變更、串流多種斷行／中文／casefold／超長行／尾端無效資料、重疊 Root、備份輪替／連結拒絕、完整性、背景錯誤與重試測試通過。
- 真實 Task Scheduler 臨時工作註冊、重複註冊、匯出設定驗證、移除成功，測試工作已清除。未執行登入觸發。
- 完整性雜湊 `fcc85a69ec0ad82518e4f8964f60c45e31787957782a0fc9c1b0c44e82d61b9b` 與既有供應商 SPDX 一致；未更換成品。

## 效能實測

開啟 tracemalloc 的 10,000 個固定文字檔：首頁 13,476.39 ms、第二頁 0.96 ms、其餘分頁 22.99 ms；完整翻頁主掃描次數為一。搜尋讀取 160,000 bytes。tracemalloc 會增加測量成本，數據不是日常無追蹤模式速度保證。

64 MiB 超長單行：原整檔讀取／解碼／splitlines 比較流程 Python 峰值 134,243,318 bytes（約 128 MiB）、119.89 ms；串流搜尋峰值 285,612 bytes（約 0.27 MiB）、271.48 ms，實讀 67,108,864 bytes。此案例記憶體明顯降低，但速度較慢。

## 真實通道檢查

使用現有 DPAPI 金鑰進行唯讀 `admin tunnels get` 成功。現有程序健康檢查：`/healthz` 200 live、`/readyz` 200 ready；`health --require-control-plane-poll --json` 回報 ok。

以上僅證明程序／準備狀態與控制平面輪詢成功，不能替代遠端工具呼叫。Responses API 固定測試文件的呼叫曾被自動核准審查拒絕，等待使用者明確同意傳送該文件至 OpenAI 後再執行。

## 未完成的真實情境

登入／登出與 30 秒內連線、真實斷網復原、睡眠／喚醒、Explorer 重啟、8–24 小時 soak、停止後遠端不可用及重啟恢復尚未實測。未主動登出 Windows 或中斷現有連線。
