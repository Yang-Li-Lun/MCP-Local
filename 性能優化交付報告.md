# 性能優化交付報告

日期：2026-09-17。服務 `2026.09.17.3`，MCP 契約 `4`，設定維持 `5`。驗證環境 Python 3.10.11／MCP 1.30.0／Windows。

## 完成範圍

- 新增 `read_file_ranges` 與 `find_files`，工具總數 11；`read_files` 提升至 32 檔，總輸出仍為 512 KiB。
- 多區段保留順序、重疊與重複；單次開檔且完整驗證 UTF-8／NUL／容量／檔案身分直到 EOF，不建立驗證快取。
- 最外層只檢查一次 2 MiB pretty JSON；batch 增量計算與後續略過訊息預留。
- 掃描預算逐筆精確累加，取消／期限每 32 項檢查，操作返回前再檢查。硬計數未延後。
- SnapshotBuilder 每列容量只計算一次並複製為唯讀 mapping；維持 16 MiB 單快照、64 MiB 全域、32 快照、250,000 筆上限。
- 預先計算搜尋詞 casefold 與 chunk 尾端長度，每檔相對路徑重用。
- 保留 4 worker、30 秒期限與所有路徑安全守衛；未加入檔案搜尋平行化。

## 修改檔案

產品：`operation_budget.py`、`stream_read.py`、`stream_search.py`、`snapshot_cache.py`、`local_files_mcp.py`、`workspace_reader.py`、`tool-contract.json`。
驗證：`tests/test_performance_v4.py`、`tests/test_engineering.py`、`tests/test_mcp_stdio.py`、`tests/test_workspace.py`、`remote_smoke_test.py`、`benchmark_e2e.py`、`benchmark_performance.py`、`benchmark-cases.json`、`benchmark-baseline-cases.json`。
文件：`README.md`、`README_繁體中文.md`、`介面使用說明.md`、本報告。既有 GUI／電源／連線等未提交修改保留；沒有 Git 提交或推送。
備份：`archive/backups/performance-v4-20260917-194110`；為重現舊 STDIO 另補入相依模組。下載的原工程文檔保留。

## 驗證

- `py_compile`、`git diff --check` 通過。
- `python -B verify_isolated.py`：196 項、0 failures、0 errors、0 skips，pip check 通過。主程序 audit guard 禁止正式通道／排程／狀態與真實電源變更。
- 新增 17 項：單次開檔、晚出現無效檔尾、CRLF chunk 邊界、重疊区段、容量、32／33 檔、hardlink、未知 root、十萬項精確計數、取消／期限。
- 實際 STDIO 列出工具、唯讀標記與完整 Schema 比對通過；兩個新工具均實際呼叫成功。
- MCP 操作、Workspace 包裝及直接 FileReader 的最終完整輸出檢查各為 1 次。
- 初輪失敗是舊 11 檔上限斷言，以及新 mock fixture 暫存短路徑／正規路徑不一致；均已修正，最後完整回歸全通過。

## 合成資料量測

Windows 同機、未切換電源、未清 OS cache。每檔 2,000 bytes，每 1,000 檔分目錄，另有 16 MiB 長檔。1k 為 20 次 warm call；10k／50k 各 10 次。max_scan_files=100000、max_scan_entries=300000、max_file_bytes=64 MiB。JSON 保留 first/min/p50/p90/p95/max。

|檔案數|list 首頁 p50 / p95 ms|find 前段 p50 / p95 ms|find 不存在 p50 / p95 ms|list / find 前段處理項目|
|---:|---:|---:|---:|---:|
|1000|99.37 / 110.43|3.50 / 6.55|168.42 / 229.62|1001 / 2|
|10000|1472.71 / 1900.88|2.90 / 4.37|1324.29 / 1466.07|10010 / 11|
|50000|4230.09 / 4985.43|5.32 / 6.79|6817.57 / 8414.91|28826 / 51|

**50k list 在 28,826 項掃描左右碰到 16 MiB 快照上限，並非完整五萬檔清冊。** find 不存在走完 50,050 項；前段 find 的處理項目較截斷 list 仍減少超過 99%。scanned_entries 是處理項目；每個目錄仍先收集／排序 DirEntry，並非磁碟列舉次數。

|場景|四次單段 p50 / p95 ms|單次多段 p50 / p95 ms|p50 比率|
|---|---:|---:|---:|
|1000 檔 fixture / 16 MiB|448.68 / 529.98|126.57 / 136.90|28.2%|
|10000 檔 fixture / 16 MiB|470.32 / 534.37|111.14 / 124.89|23.6%|
|50000 檔 fixture / 16 MiB|378.82 / 413.75|91.46 / 102.16|24.1%|

三組多區段均 `open_checked=1`、`bytes_read=16777216`，p50 均低於四次讀取的 40%。
1k checkpoint：2008 → 70，下降 96.5%。十萬文字檔 mock 另驗證 100,100 項精確計數與 checkpoint <60,000 次（相較逐項兩次下降至少 70%）。
32 檔一次呼叫成功，回應 25,087 bytes；大型中文資料與 16 重疊區段測試仍維持 512 KiB。batch 主要收益是減少 MCP 往返，不保證本機每次耗時下降。

## 小型 local／STDIO

固定 shared/README.md，初測 20 次；因 p95 波動增加至 100 次。獨立跑的部分 STDIO p95 超過 10%，再用兩條連線交錯呼叫控制時間漂移；原先較差樣本仍保留。下表 local 為 100 次，STDIO 為 100 次交錯。

|模式／案例|舊版 p50 / p95 ms|新版 p50 / p95 ms|
|---|---:|---:|
|local list_directory|0.65 / 1.07|0.57 / 1.00|
|local read_file|0.73 / 1.24|0.68 / 1.30|
|local search_texts|1.13 / 1.67|1.00 / 1.60|
|STDIO 交錯 list_directory|3.73 / 4.89|3.58 / 4.53|
|STDIO 交錯 read_file|3.82 / 4.86|3.84 / 4.66|
|STDIO 交錯 search_texts|4.71 / 6.35|4.49 / 5.40|

交錯測試未發現持續超過 10% 的 p95 退化，保留優化。屬共享桌面量測，不是隔離實驗室；只完成 1k 舊版比較，10k／50k 沒有完整舊版基線。

## 並行與記憶體

|並行|warm 批次 p50 / p95 ms|成功 / RESOURCE_BUSY（20 批）|
|---:|---:|---:|
|1|81.25 / 86.53|20 / 0|
|4|351.74 / 378.08|80 / 0|
|8|344.02 / 376.77|80 / 80|

50k 程序 Peak Working Set：126.14 MiB，含 runtime／fixture／快照，非純工具增量。
16 MiB 單段 instrumentation：{"read_bytes": 16777216, "stat_calls": 11, "lstat_calls": 8, "direntry_stat_calls": 0, "elapsed_ms": 205.426, "python_peak_bytes": 228997}。此 elapsed 含 tracemalloc 與 hooks，不與 warm latency 混比。

## 重現

```powershell
.\.venv\Scripts\python.exe -B verify_isolated.py
.\.venv\Scripts\python.exe -B benchmark_performance.py --source . --files 10000 --output performance-v4-10k.json
.\.venv\Scripts\python.exe -B benchmark_performance.py --source . --files 50000 --output performance-v4-50k.json
.\.venv\Scripts\python.exe -B benchmark_e2e.py --mode local --root shared --cases benchmark-cases.json --output performance-v4-local.json
.\.venv\Scripts\python.exe -B benchmark_e2e.py --mode stdio --root shared --cases benchmark-baseline-cases.json --baseline-source archive/backups/performance-v4-20260917-194110 --repeats 100 --output performance-stdio-interleaved.json
```

benchmark_performance 保留原 run()、instrumentation 及 --legacy fixture；另提供 measure_concurrency(reader, repeats=20)。

## 連線與已知限制

舊版連線已由使用者停止。啟動曾被自動審核要求明確共享範圍確認；使用者確認既有 D:\codee、D:\download 與既有 OpenAI 通道後，已由 autostart.py 正常啟動新版。沒有繞過審核，也未修改共享範圍、金鑰或電源模式。

2026-09-17 20:04:49 新 Tunnel 與 MCP 程序建立。/healthz=200、/readyz=200、MCP main STDIO probe=ok，並觀察到成功的控制平面 poll。安全摘要存於 performance-connection-check.json。連線目前由系統匣 GUI 管理。
HTTPS／ChatGPT 11 工具正式端到端尚未驗收；健康檢查 200 與本機 STDIO 不等同遠端驗收。既有 tunnel_id 沒有可供通用 HTTPS benchmark 的直接 MCP URL，因此 HTTPS 延遲未量測。
尚未執行 HDD／網路磁碟／各電源模式矩陣、50k 每檔 1 MiB 資料集及可選平行搜尋研究。
