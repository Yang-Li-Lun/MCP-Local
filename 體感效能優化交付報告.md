# 體感效能優化交付紀錄

日期：2026-09-13。依使用者指定方案實作；原始 Downloads 文件保留。

## 已完成

- 新增 search_texts：1～10 個字面搜尋詞，每詞 1～200 字元；limit_per_query 1～100、context_lines 0～3。一次 walk、每檔一次 open/read、同 chunk 多詞比對。每工具最多 200 筆命中，命中 JSON 累計最多 100000 UTF-8 bytes，沿用 max_scan_bytes、entries、operation timeout 與整體 2 MiB JSON 上限。重複搜尋詞依輸入順序各自回報。
- 保留全文 UTF-8/NUL、檔案大小與 open_checked 前後身分檢查；無效檔尾會丟棄所有命中。
- 更新工具說明：專案先用 project_context，多檔用 read_files，探索用 list_directory，多詞用 search_texts，完整遞迴才用 list_files。
- 既有八工具加 search_texts 共九工具；tool-contract.json、遠端煙霧測試與 STDIO 驗收同步更新。service_version 2026.09.13.2、contract_version 3。
- 新增 benchmark_e2e.py：local、stdio、https 三模式；每案例至少 20 次，持續 session/client，setup 與 first_call 分列，暖連線 min/p50/p90/p95/max。僅寫統計，不寫讀取內容、URL 或認證。

## 已執行驗證

- Python 3.10.11、MCP 1.30.0。
- py_compile：local_files_mcp.py、workspace_reader.py、stream_search.py、benchmark_e2e.py 通過。
- unittest discover：128 tests，零失敗、零 skip。
- verify_isolated.py：128 tests，零失敗、零 skip，pip check 通過。
- 補驗 search_texts 實際 STDIO 呼叫：3 個 MCP smoke tests 通過。
- 新測試覆蓋單次走訪/開檔、casefold/前後文等價、跨 64 KiB 區塊命中、無效 UTF-8/NUL 檔尾、總命中上限、掃描 bytes、無效參數與路徑逃逸。

## 量測

benchmark-cases.json 使用本專案與 shared 內的明確案例；每案例 20 次暖呼叫，未清 OS cache。原始結果見 benchmark-local.json、benchmark-stdio.json。下列為 p50 / p95，單位 ms。

| 案例 | 本機 | STDIO |
| --- | --- | --- |
| 小檔 read_file | 0.75 / 1.18 | 3.03 / 3.62 |
| 四檔 read_files | 4.80 / 9.33 | 7.80 / 8.39 |
| project_context | 2.91 / 3.44 | 6.44 / 6.92 |
| search_text 命中 | 1.05 / 1.36 | 3.91 / 4.56 |
| search_texts 四詞 | 1.14 / 1.50 | 3.65 / 4.71 |
| list_directory 專案 | 3.51 / 4.06 | 6.02 / 8.13 |
| list_files 專案 | 7.36 / 8.53 | 10.00 / 11.12 |

搜尋使用小型固定 shared；不可推論大型 Root 或遠端效能。尚未量測 HTTPS、最終用戶端、大型 Root 20 次或相同四檔逐次讀取對照；不宣稱已達實際流程 50% 降幅。

執行例：

```powershell
.\.venv\Scripts\python.exe -B benchmark_e2e.py --mode local --root . --cases benchmark-cases.json --output benchmark-local.json
.\.venv\Scripts\python.exe -B benchmark_e2e.py --mode stdio --root . --cases benchmark-cases.json --output benchmark-stdio.json
```

HTTPS 模式需明確 MCP HTTPS 端點，並將案例調整為該服務授權的 root_id/相對路徑。認證只從 MCP_BENCHMARK_BEARER 環境變數讀取。現行 tunnel-id 不是可直接傳給 streamable_http_client 的 HTTPS URL。

## 暫緩與待驗收

- P2 稀疏索引/驗證快取：方案提議依 metadata 免掃全文，與 AGENTS.md「串流讀取需在目標行後繼續驗證全文，不可依賴大小或 mtime 快取作為完整性依據」衝突。保留每次全文驗證；未加入會跳過驗證的快取。
- 可選 inspect_project 暫不加入，遵循方案先量測再決定的順序。
- 最終用戶端工具快取刷新與九工具可見性未驗證；本次可用連接器工具不含此本地服務，不能以 STDIO 代替。
- 重新啟動最初遭自動核准審查拒絕；使用者隨後明確同意連線。2026-09-13 19:32（Asia/Taipei）重新核對分享範圍與目的地後，既有排程啟動成功（START_EXIT 0），確認新 tunnel-client 與 local_files_mcp 子程序均已執行。程序啟動不等於最終用戶端工具驗收完成。
- 已確認既有範圍 main = D:\codee，目的地 tunnel_example。未變更正式根目錄、排程設定、憑證或供應商二進位檔。
